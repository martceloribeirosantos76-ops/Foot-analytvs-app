export interface FootballDataProvider {
  // Implementar aqui o adaptador do provedor de dados escolhido.
  // Não coloque a chave da API no código-fonte.
}

export class ExternalFootballProvider implements FootballDataProvider {
  // Vazio de propósito na V1: evita acoplar o sistema a um fornecedor específico.
}
